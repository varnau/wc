#include <stdio.h>
#include <stdlib.h>

#include "wc.h"

//----------------------------------------------------------

void main(int arg, char *argv[]) {
  int k = 10;
  wc_t *wc = wc_new(k);

  wc_display(wc);

  wc_free(wc);
}
//----------------------------------------------------------
//----------------------------------------------------------
